Run `make` to compile the program.

Run `make test` to test the program against the example script provided in the question.

Run `make full` to test the program against the full question and output.

Run `make clean` to remove compiled files.
